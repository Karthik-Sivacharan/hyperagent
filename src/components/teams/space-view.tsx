"use client";

// Placeholder until the actors builder lands the office view
// (docs/plans/2026-09-11-teams-space-v1.md). Keeps `?view=space` routable.
export function SpaceView() {
  return <div className="flex flex-1 items-center justify-center text-md text-foreground-low">Office</div>;
}
