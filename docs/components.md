# The component system

The clone's UI is one component set on the brand tokens, organised in four tiers under `src/components/`. This is the reference for it: the tiers and the rules, the map of every primitive and pattern with its live evidence, how to add a component, the live UI the clone does not render yet, and the decisions the sweep left to the design owner. Written 2026-09-07 at the end of the component system sweep (`docs/plans/2026-09-07-component-system-sweep.md`); `src/components/components.test.ts` keeps the rules true.

## 1. Tiers and rules

| Tier | Path | What lives there |
|---|---|---|
| Primitives | `src/components/ui/` | 29 shadcn-style primitives on the brand tokens (pills, tints, hairlines, glass shadows) behind the phase-1 variant and size API, so page layouts keep their metrics. The only place `radix-ui` and `cmdk` are imported. Every one carries a `data-slot` (the name the live site emits, where it emits one). |
| Patterns | `src/components/patterns/` | Composites of primitives used by two or more pages with the same intent: `PageHeading`, `SearchInput`, `EmptyState`, `ShowArchivedSwitch`. A pattern earns its file only when a shape recurs; a single use stays in the page. |
| Shell | `src/components/app/`, `src/components/composer/` | The sidebar, the frame, the account and section menus, the ⌘K palette, the composer and its menus. Built from the primitives; shell-local composites (the sidebar group header, the rail tooltip) stay in the shell. |
| Pages | `src/components/<page>/` and the routes in `src/app/(app)/` | Layout, data wiring and composition of the tiers above. Never a raw control. |

The rules, each locked by its own `it` in `src/components/components.test.ts` (`npm test`):

1. `radix-ui` and `cmdk` are imported only by files under `src/components/ui/`. A page that needs a radix part gets it through a primitive; if no primitive wraps it yet, add one (§3).
2. No raw `<button>`, `<input>`, `<textarea>`, `<select>` or `<label>` in JSX under `src/components/**` (outside `ui/`) or `src/app/**`. The one allowed shape is the rendered element of an `asChild` primitive, where the raw tag is the primitive's direct child: `src/components/thread/option-cards.tsx` renders `<Card asChild size="none"><button …>` because the card itself is the pressable element. The test also allow-lists `src/app/design/brand/page.tsx`, whose three raw buttons are the motion specimens of the swatch page (press, hover and focus on a bare element), not controls the app ships.
3. `src/design/brand/ui/` does not exist and nothing imports `@/design/brand/ui` or `@/design/brand/utils`. The prototype's verbatim component copies were retired in the sweep and stay in git history.
4. Every `data-slot="<name>"` value in `docs/reference/pages/*.html` and `docs/reference/overlays/*.html` has a `data-slot="<name>"` under `src/components/ui/` or `src/components/patterns/` (57 values today, all defined). The assertion message lists any missing name, so a new capture that introduces a slot fails the suite until a primitive names it.
5. Every file under `src/components/ui/` contains at least one `data-slot=`.

Conventions every primitive follows (copy them in a new one): `data-slot` on the root and on every named part; a cva `variants` object where there are two or more looks, mirrored as `data-variant` / `data-size`; `cn()` last so `className` is the escape hatch; `asChild` through `Slot.Root` where a link or list item can be the element; Tabler icons with `aria-hidden="true"`; motion as `transition-[…] duration-(--duration-fast) ease-out-quart` with `motion-safe:active:scale-(--scale-press)`; fills as tints, edges as `shadow-edge` or `border-border-subtle`; no raw colours (`npm run brand:lint-tokens`).

## 2. Component map

Live evidence comes from the 2026-09-07 sweep of hyperagent.com (the DOM dumps under `docs/reference/`). "Used by" lists the directories under `src/components/` (and `src/app/`) that import the file.

### Primitives (`src/components/ui/`)

| Component | File | Variants, sizes, props worth knowing | Live evidence | Used by |
|---|---|---|---|---|
| `Avatar`, `AvatarImage`, `AvatarFallback`, `AvatarBadge`, `AvatarGroup`, `AvatarGroupCount` | `avatar.tsx` | `size` default (32px) / sm (24) / lg (40) as `data-size`; the rim is `after:shadow-edge` | The live account row is a bare `img.rounded-full.size-8` with no slot; the clone keeps its two avatars as `<img>` (content, not controls; the rim would add a pixel) | none yet |
| `Badge` | `badge.tsx` | `variant` default, secondary, destructive, outline, ghost, link, brand, success, warning, info; fixed `h-5 px-2 text-xs`; `asChild` | `span[data-slot=badge]`: count pills on inbox, learning and skills; `MCP` / `NATIVE` on integrations | composer, marketplace, settings |
| `Button` | `button.tsx` | `variant` default (ink), brand, destructive, outline, secondary, ghost, tint, chip, link; `size` default (36px), xs (28), sm (32), lg (40), pill (32, 12px padding kept beside an icon), icon (36), icon-2xs (22), icon-xs (24), icon-sm (32), icon-lg (40), none (the caller owns height, padding and gap); `asChild`; `data-variant`, `data-size` | `button[data-slot=button][data-variant][data-size]` on every page (196 in the dumps) | the shell, the composer, every page directory, the swatch toggle; `ui/input-group`, `ui/sheet` |
| `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardAction`, `CardContent`, `CardFooter` | `card.tsx` | `size` default (16px spacing) / sm (12) / none (the page keeps its own padding); `variant` default / interactive (the hover lift); `asChild` for a link, list item or button shell | `div[data-slot=card]` with `card-header`, `card-title`, `card-description`, `card-content` on settings, integrations (102), the skills grid, learning | home, learning, marketplace, settings, skills, thread, threads |
| `Checkbox` | `checkbox.tsx` | no variants; slots `checkbox`, `checkbox-indicator`; ink when checked | `button[role=checkbox][data-slot=checkbox]` on /memories | memories |
| `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent` | `collapsible.tsx` | pass-through, no classes | Live collapsibles are bare radix (`button[aria-expanded][data-state]`, no slot): the skills "Discover" header, the learning rows, the thread reasoning blocks | learning, skills |
| `Command`, `CommandDialog`, `CommandInput`, `CommandList`, `CommandEmpty`, `CommandGroup`, `CommandItem`, `CommandShortcut`, `CommandSeparator` | `command.tsx` | `CommandDialog` `title`, `description`, `showCloseButton`; 48px input row; the only `cmdk` import | `command`, `command-input-wrapper`, `command-input[role=combobox]`, `command-list[role=listbox]`, `command-group`, `command-item[role=option]` in the ⌘K dialog | app (search palette) |
| `ContextMenu` family (15 exports) | `context-menu.tsx` | `ContextMenuItem` `variant` default / destructive, `inset`; the same content and item strings as the dropdown menu | `context-menu-trigger` on every thread row and card; `context-menu-content`, `-item`, `-separator`, `-sub-trigger` on right-click | app (`ThreadContextMenu` in thread-menu.tsx, which wraps the sidebar rows and the home and threads cards) |
| `Dialog` family (10 exports) | `dialog.tsx` | `DialogContent` and `DialogFooter` `showCloseButton`; the overlay scrolls and the content sits `m-auto`, as on the site | `dialog-overlay`, `dialog-content[role=dialog]`, `-header`, `-title`, `-description`, `-footer`, `-close`: search, the teams Join / Create dialogs, the integrations Connect / Manage dialogs, the thread Live-mode dialog | `ui/command` (the ⌘K dialog); the other dialogs are not cloned yet (§4) |
| `DropdownMenu` family (15 exports) | `dropdown-menu.tsx` | `DropdownMenuItem` `variant` default / destructive, `inset`; content `sideOffset` 4; radio and checkbox items, each with `indicator` (`check` / `dot` by default, `none` to drop the left gutter so the item draws its own on-state and keeps `role="menuitemcheckbox"` / `menuitemradio` and `aria-checked`); the content classifies the close as pointer or keyboard: a keyboard close keeps Radix's own focus return, so Escape and keyboard selection put the ring and the tooltip back on the trigger, while a pointer close leaves focus where the pointer put it. Restoring focus on a mouse dismissal is what left a composer pill wearing its focus ring and holding its tooltip open, since each pill is both a menu trigger and a tooltip trigger and a tooltip opens on focus. The cost is that a mouse close does not restore the tab position | `dropdown-menu-trigger`, `-content[role=menu]`, `-item[role=menuitem]`, `-label`, `-separator`, `-sub-trigger`, `-sub-content` in more than 25 menus | agents, app, composer, home, memories, settings, skills, thread, threads |
| `IconTile` | `icon-tile.tsx` | `size` xs (20px, a letter) / sm (24) / lg (48) / xl (64); `shape` rounded (8px) / soft (14) / circle; `tone` raised (canvas plus hairline) / tint / brand; `data-size`, `data-tone`; the icon inside keeps the caller's size | `div[data-slot=icon-tile]` on the marketplace and skills cards, the projects card, integrations, the thread sub-agent block | composer, marketplace, settings, patterns (`EmptyState`) |
| `Input` | `input.tsx` | `variant` default (32px field, `rounded-lg`, `border-input`) / bare (no box: menu search fields, hidden file inputs); `data-variant` | `input[data-slot=input]` on every list page's search and in the teams dialog | agents, app (sidebar), composer; `ui/input-group` |
| `InputGroup`, `InputGroupAddon`, `InputGroupButton`, `InputGroupText`, `InputGroupInput`, `InputGroupTextarea` | `input-group.tsx` | addon `align` inline-start / inline-end / block-start / block-end; button `size` xs / sm / icon-xs / icon-sm; the pill field is 32px, the control carries `input-group-control` | none: the site's search is a bare `input.pl-8` with an absolute icon (the same box) | patterns (`SearchInput`) |
| `Kbd`, `KbdGroup` | `kbd.tsx` | none; a 20px keycap on `bg-tint-10` | `kbd` elements without a slot in the sidebar Search row and the ⌘K item hints | app (sidebar, search palette) |
| `Label` | `label.tsx` | shadcn's string without `leading-none` (the 20px line sets the header row height on projects and library) | `data-slot=label` beside the Show Archived switch and in the teams dialogs | patterns (`ShowArchivedSwitch`) |
| `NavItem` | `nav-item.tsx` | `tone` default / muted; `active`; `asChild` for links; `data-active` | The sidebar rows: `a` or `button` `.flex.w-full.items-center.gap-2.px-3.5.py-1.5.text-sm`, no slot of their own (they are `tooltip-trigger`s) | app (sidebar) |
| `Overline` | `overline.tsx` | `asChild` (a heading or a menu label carries the slot); layout in `className` | `h2` or `div` `[data-slot=overline]`: the settings section heads, the agent picker, the thread-settings Tools groups, the version-history popover | app, composer, settings, threads |
| `Popover`, `PopoverTrigger`, `PopoverContent`, `PopoverAnchor`, `PopoverHeader`, `PopoverTitle`, `PopoverDescription` | `popover.tsx` | content `align` center, `sideOffset` 4; no width or padding by design | Live popovers are `div[role=dialog]` with no slot: the "Set up your agent" and "More…" chips, the memories scope, the version history | none yet: the two home chip popovers render as dropdown menus since the sweep (the same box, `role=menu` instead of `role=dialog`); switch them back if the role matters |
| `ScrollArea`, `ScrollBar` | `scroll-area.tsx` | `viewportRef`, `viewportProps` (reach the viewport for `onScroll` and refs); `ScrollBar` `orientation` | `scroll-area` + `scroll-area-viewport` on the thread message column | thread |
| `Select` family (10 exports) | `select.tsx` | `SelectTrigger` `variant` outline (the Button outline look) / tint (the toolbar pill), `size` sm (32px) / default (36); content `position="popper"`, `sideOffset` 4; the item indicator in the accent | `select-trigger[role=combobox]`, `select-value`, `select-content[role=listbox]`, `select-item[role=option]`, `select-item-indicator`, `select-group`, `select-label`, `select-separator`: the four library selects, the skills sort, the Live-mode dialog | library, skills |
| `Separator` | `separator.tsx` | `orientation`, `decorative`; `bg-border-subtle` | `div[data-slot=separator][role=separator]` in the expanded learning rows and the projects card | none yet (those populated states are not cloned, §4) |
| `Sheet` family (8 exports) | `sheet.tsx` | `side` top / right / bottom / left; `showCloseButton` | none on the live site | none yet |
| `Skeleton` | `skeleton.tsx` | none; `animate-skeleton` on `bg-tint-10` | none on the live site | none yet |
| `Switch` | `switch.tsx` | `size` default (18×32) / sm (14×24, the thread-settings Fast inference row); `data-size` | `button[role=switch][data-slot=switch]` + `switch-thumb`: thread settings, memories filters, projects, library | composer, patterns (`ShowArchivedSwitch`) |
| `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent` | `tabs.tsx` | `TabsList` `variant` default (pill track) / line (underline); `orientation` | `tabs`, `tabs-list[role=tablist]`, `tabs-trigger[role=tab]`, `tabs-content` on /skills and /memories | memories, skills |
| `Textarea` | `textarea.tsx` | `variant` default (`rounded-xl` field) / bare (the composer editor); `data-variant` | `data-slot=textarea` in the teams Create and Live-mode dialogs; the live composer itself is a TipTap `[role=textbox]` | composer; `ui/input-group` |
| `Toggle` | `toggle.tsx` | `variant` default / outline; `size` default (32px) / sm (28) / lg (36); the on-state reads `aria-pressed`, `aria-checked` and `data-state` | none as a slot: the live layout switch is a `role=group` of `aria-pressed` ghost buttons | `ui/toggle-group` |
| `ToggleGroup`, `ToggleGroupItem` | `toggle-group.tsx` | `spacing` (0 = the joined pill track), `orientation`, plus `toggleVariants` | none as a slot (see `Toggle`): List / Grid / Board on home and threads, Grid / List on skills | home, skills, threads |
| `TooltipProvider`, `Tooltip`, `TooltipTrigger`, `TooltipContent` | `tooltip.tsx` | `sideOffset` 6; the ink card, no arrow | `tooltip-trigger` (333 in the dumps, on buttons, links, spans and one svg), `tooltip-content[data-side]` | the root layout (provider), app, composer, home, learning, library, marketplace, memories, settings, threads |

### Patterns (`src/components/patterns/`)

| Component | File | Props | Live evidence | Used by |
|---|---|---|---|---|
| `PageHeading` | `page-heading.tsx` | `title`, `subtitle`, `actions`, `className` (threads and marketplace pass `flex-row flex-wrap`), `titleClassName` (marketplace `text-3xl`) | The `h1` + description + primary button row on every resource page | agents, inbox, learning, library, marketplace, memories, projects, settings (`SettingsPageHeader`), skills, teams, threads |
| `SearchInput` | `search-input.tsx` | input props plus `className`, `inputClassName`, `iconClassName` (marketplace and skills drop the icon to the third tier), `children` for a trailing control | `input[data-slot=input].pl-8` with a search glyph on threads, skills, memories, agents, library, marketplace, integrations | agents, library, marketplace, memories, settings, skills, threads |
| `EmptyState` | `empty-state.tsx` | `icon`, `title`, `description`, `action`, `variant` bubble (a round `IconTile`, xl heading) / plain (a bare icon, lg heading) | The icon + heading + copy + CTA empty states on projects, agents, library, memories | agents, library, memories, projects |
| `ShowArchivedSwitch` | `show-archived-switch.tsx` | `checked`, `onCheckedChange`, `id` | `switch` + `label` "Show Archived" on projects and library | library, projects |

### Slots the clone emits that the live site does not

`toggle-group`, `toggle-group-item`, `input-group`, `input-group-addon`, `input-group-control`, `popover-content`, `collapsible`, `collapsible-trigger`, `collapsible-content`. The live site builds the same boxes without a slot: its layout switch is a `role=group` of `aria-pressed` ghost buttons, its search field a bare input with an absolute icon, its popovers unnamed `role=dialog` panels and its collapsibles bare radix. The clone's primitives wrap the same radix parts and name them, the rendered boxes are the same, and the slot lock checks only the live direction (every live slot has a local definition), so an extra local slot costs nothing.

## 3. How to add a component

1. `npx shadcn@latest add <name> --yes`. The file lands in `src/components/ui/` (`components.json`). If shadcn has no such component (the sweep hand-wrote `overline`, `icon-tile` and `nav-item`), write a 20-to-50-line file in the same shape: `data-slot`, a cva `variants` object, `cn()` last, `asChild` where it makes sense.
2. Convert the icons: the CLI emits `lucide-react`; rename each to its Tabler name per `docs/brand/icons.md` and add `aria-hidden="true"`. `npm test` rejects a lucide import.
3. Fix the `cn` import if the CLI wrote `from "cn"` or another wrong path: it must be `import { cn } from "@/lib/utils"`.
4. Re-skin on the brand strings: read "The brand in ten lines" in `docs/brand/reskin-conventions.md` and the neighbouring primitives, then replace shadcn's classes with the tints, hairlines, pills, radii and motion tokens. Take the class string from a route that already renders the shape when one exists (the sweep's `select.tsx` came from `/library`, `checkbox.tsx` from `/memories`).
5. Keep the `data-slot` names shadcn emits (they are the ones the live site emits) and keep the phase-1 heights (28 / 32 / 36 / 40) so nothing moves.
6. Run `npx tsc --noEmit`, `npm run lint`, `npm run brand:lint-tokens` and `npm test` (the slot lock and the raw-control rule), then the screenshot diff: capture the affected routes before and after with `scripts/dev/screenshot-pages.mjs` and compare pixel by pixel (the capture is deterministic, so any differing pixel is a real change).
7. Add a row to the map above.

## 4. Live UI the clone does not render

The 2026-09-07 sweep found UI on hyperagent.com that the 2026-09-06 capture did not have and the clone lacks. These are clone gaps, not component gaps: every primitive they need exists in `ui/` or is listed here. The two shadcn primitives the live site uses that `ui/` does not have are `RadioGroup` (the Live-mode dialog's channel picker) and `Progress` (the account menu's plan card, `role=progressbar`); add them with `npx shadcn@latest add radio-group progress` when those features are cloned. The dump for each item is under `docs/reference/overlays/`.

- **Starred sidebar group** between the nav block and Agents, with a project-folder glyph at the end of the row (a `tooltip-trigger` naming the project) and menus that read "Unstar thread". Dumps: `sidebar-starred-row-menu.html`, `sidebar-starred-row-context-menu.html`, `tooltip-project-tag.html`.
- **Thread rows** on home and /threads: a project tag (folder glyph plus name), a star toggle beside the `…` button (a ghost icon button with a filled star when starred) and a swipe-to-archive layer behind the row. Dumps: `home-thread-card-menu.html`, `home-thread-card-context-menu.html`, `threads-card-menu.html`.
- **Generated grid covers** on the home Grid view: blurred radial-gradient blobs, a vignette and an SVG noise layer per card. Dump: `home-grid-view.html`.
- **Execute replaces Plan** on the composer pill (`aria-label="Execution mode: Execute"`, a bolt icon); the menu is unchanged. Dumps: `composer-plan-menu.html`, `composer-plan-menu-execute.html` (captured when the pill still read Plan).
- **Integrations sub-menus**: the composer `+` menu gained Integrations (GitHub, "Add other integrations"); thread settings has Integrations rows with Connect buttons and switches, and Tools grouped by `overline`. The home "Connect your integrations" chip is gone. Dumps: `composer-add-menu-integrations.html`, `composer-thread-settings-integrations.html`, `composer-thread-settings-tools.html`.
- **Referral chip** ("Earn $100 per referral · Invite friends") above the account row. Seen once in an accessibility snapshot, never captured as DOM.
- **The thread-detail layer** on a populated thread: the Live pill's "Set up Live mode" dialog (radio group, select, textarea), reasoning collapsibles, tool-call chips (`span[role=button]` with a verb, a detail and an expandable `pre`), the document-update block, the sub-agent block (beam avatar, model chip, an `icon-tile` check), the suggested-learnings panel (badge count, typed icons, a confidence dot), the header model pill with a context-usage ring and a menu holding a Context Window panel, the title menu with Share / Fork / Thread settings, and the artifact side panel (Carousel / Tile / Windows group, sort menu, card chrome with a version-history popover, a sortable dock). Dumps: `thread-live-mode-dialog.html`, `thread-title-menu.html`, `thread-title-menu-move-to-project.html`, `thread-model-pill-menu.html`, `thread-sort-artifacts-menu.html`, `thread-artifact-tile-menu.html`, `thread-doc-version-history-popover.html`. The message-level blocks (reasoning, tool calls, document updates, sub-agents, suggested learnings) have no dump yet; capture one before cloning them. **The artifact side panel's Carousel layout is built, static and not wired (2026-09-10):** `src/components/workspace/` (wallpaper, glass toolbar, cards, dock), previewed beside the thread view at `/design/workspace`, ground truth `thread-workspace-carousel.html`. Its Tile and Windows layouts, menus, Spotlight and Browser view are not.
- **Populated states** the old dumps had empty: /projects (a project card with stats, an overline, a thread row, a `separator` and an actions menu: `projects-actions-menu.html`), /inbox (a "4 learnings" row with a badge; it links to /learning), /learning ("3 pending", type toggles, expandable rows: `learning-thread-row-expanded.html`), /skills (a sort select, Filters, row actions, a sortable table header: `skills-sort-select.html`, `skills-filters-menu.html`, `skills-row-actions-menu.html`, `skills-discover-collapsed.html`), /memories (More actions, the scope popover, Filters with sub-menus and switches: `memories-more-actions-menu.html`, `memories-scope-popover.html`, `memories-filters-menu.html`), /library (the four selects: `library-select-sort.html`, `library-select-type.html`, `library-select-visibility.html`, `library-select-source.html`), /teams (the Join and Create dialogs: `teams-join-dialog.html`, `teams-create-dialog.html`), /settings/integrations (the Connect and Manage dialogs, the Add menu: `integrations-connect-dialog.html`, `integrations-manage-dialog.html`, `integrations-add-menu.html`), /agents (Filters: `agents-filters-menu.html`), /threads (the Board view, Sort and Filters: `threads-board-view.html`, `threads-sort-menu.html`, `threads-filters-menu.html`).
- The collapsed sidebar's hidden **Pin sidebar** button (`sidebar-collapsed.html`).

## 5. Decisions left to the design owner, and the one pixel change

Two inconsistencies the audit found were left as they are because fixing them moves pixels, and the sweep's contract was zero pixel change:

- **Toolbar buttons and pressed chips** (audit §6.7). The 36px toolbar buttons are spelled four ways: threads `size="sm"` plus `h-9 gap-2`, memories `size="sm"` plus `h-9` (the 6px gap), agents `size="sm"` plus `h-9 shrink-0 gap-2`, skills the default size plus `h-9 gap-2` (16px padding, 12px beside an icon, so the skills Filters button is 2px wider than the threads one). The pressed filter chips use two tint strengths: threads `bg-tint-20`, library and the sidebar `bg-tint-15` lifting to `bg-tint-20` on hover. A `size="toolbar"` on `Button` and one pressed style on `outline` / `ghost` would unify them; each moves a metric or a tint step on one page.
- **Glass fills on hover-revealed actions** (audit §6.12). Three glass looks for one idea: the home thread card's actions (`bg-background/80 backdrop-blur-sm` on an outline icon button), the threads card's actions (`bg-surface-elevated/80 shadow-edge backdrop-blur-sm` on a ghost icon button) and the thread view's scroll-to-bottom pill (`bg-surface-elevated/90 shadow-md ring-1 ring-border-subtle backdrop-blur-sm`). One glass variant would unify them and move a fill on one of the two lists.

The one deliberate pixel change the sweep made: the sidebar group headers Agents and Recent threads now render the brand's 12px caps role like Resources. Before the sweep they were sentence case by accident: the browser's button stylesheet reset `text-transform` on the raw `<button>` before the caps role applied. Commit "fix(shell): caps on every sidebar group header" (`572a136`); it touches the sidebar on every route, so every route differs from the pre-sweep baseline in exactly those two labels.
